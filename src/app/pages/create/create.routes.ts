import { Routes } from '@angular/router';

import { CreateComponent } from './create.component';

export const CREATE_ROUTES: Routes = [
  {
    path: '',
    component: CreateComponent,
    title: 'Flag Archive - Create',
  },
];
